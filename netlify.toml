[build]
  Command = "npm run build"
  Functions = "lambda"
  Publish = "public"

[[headers]]
  for = "/*"
  [headers.values]
    cache-control = '''
    max-age=0,
    no-cache,
    no-store,
    must-revalidate'''
